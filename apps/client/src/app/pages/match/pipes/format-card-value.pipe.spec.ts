import { FormatCardValuePipe } from './format-card-value.pipe';

describe('FormatCardValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCardValuePipe();
    expect(pipe).toBeTruthy();
  });
});
